git init  (to initialize the empty git repo)
git config --global user.name “pavan”
git config --global user.email pavankmdamu01@gmail.com
git config --list
touch file1
ls 
git add file-name
git add .
git add *
git add -A
git status
git commit –m “test commit” file-name
git commit –m “label”
git log
git show shacode
git reset HEAD file-name (get back the file from staging/index to workspace)
git reset --soft cid (get back data from local repo to staging area(You should take the below sha code if you want upper’s commit data ))
git reset –mixed cid (It brings data from local repo to workspace)(Whichever data hold in staging area that will gone to workspace area when you fetch the data from local repo to workspace area)
git commit –am “label” (whenever shows as modify file in untracked status you can send data from workspace to local repo)
git commit --hard shacode (its delete the id and data)
git log
git log --oneline
git log -2 (It shows the Latest 2 logs)
git log --oneline -4
git log --author=pavan
git log --author=pavan --oneline -2 
git log –since=yyy-mm-dd
git log –until=yyy-mm-dd
Master means by default branch name.
git branch (Shows the no.of branches)
git branch new-branch (to create new branch)
git checkout b-name ( switch to another branch)
 cat > fname (To create and write the content)
ctrl+d
cat >> fname (To add data)
cat fname (u can read the content of the file)
git merge new-branch (You should be in master branch while you merge the data)
git branch –d branch name (To delete the branch,  while deleting a branch you should be in other branch)
git checkout –b new-branch (To Checkout and new branch creates)
git pull (If incase done with any modifications in the central repo)

